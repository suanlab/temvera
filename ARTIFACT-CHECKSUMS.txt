# Deterministic artifact package for the PVLDB EA&B submission.
#
# The archive is byte-reproducible: building it twice from the same tree yields
# the same bytes, so this checksum is a claim you can falsify rather than a
# record of one build. Regenerate and compare with:
#
#   temvera artifact-package . /tmp/temvera-artifact.tar.gz
#   sha256sum /tmp/temvera-artifact.tar.gz
#
# Verify the archive's own internal manifest with:
#
#   temvera verify-artifact-package /tmp/temvera-artifact.tar.gz
#
# Contents: source, tests, docs, fixtures, experiment configs and every accepted
# sealed run. Excluded by design: data/raw (third-party corpora we download
# rather than redistribute) and compiled bytecode.

dd67fcec33f54b9d02ca3326927f4e01922197c09d37ecb7b3e4204ae8d40f9b  temvera-artifact.tar.gz
