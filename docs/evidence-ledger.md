# Evidence ledger

This ledger prevents unsupported novelty and performance claims. Add one row per
claim and prefer primary sources.

| ID | Claim | Primary source | Status | Caveat / next action |
|---|---|---|---|---|
| E-001 | Long-term memory evaluation must cover updates and abstention, not only recall. | LongMemEval, ICLR 2025 | supported | Commit `9e0b455` confirms knowledge-update and abstention task labels. QA accuracy uses a fixed GPT-4o judge; retrieval metrics explicitly skip 30 abstention cases. MIT covers the repository code, while the separately downloaded benchmark-data license remains unclear. |
| E-002 | Rank-level temporal decay can reduce retrieval quality. | vstash preprint | provisional | Reproduce; single-author preprint and dataset-dependent. |
| E-003 | Git history can serve as a transaction-time ledger. | Temvera hypothesis | unverified | Prove equivalence against relational oracle. |
| E-004 | File-front-matter provenance defense is novel. | literature search | unverified | Search security venues, MCP systems, and data lineage work. |
| E-005 | Judge-free procedural generation and lifecycle scoring alone distinguish Temvera from ForgetEval. | ForgetEval source, commit `b6053b7`, MIT | contradicted | ForgetEval already generates five seeded template families and uses deterministic top-k substring checks without an LLM judge. Temvera must distinguish exact bitemporal state/as-of semantics, operation traces, or stronger purge verification instead. |
| E-006 | Long-term conversational-memory evaluation already covers temporal reasoning, knowledge updates, and abstention. | LongMemEval, arXiv:2410.10813 | supported | Reuse its task taxonomy; do not claim these capabilities as new. |
| E-007 | Very-long conversational histories and temporal/causal questions are already evaluated by LoCoMo. | LoCoMo, arXiv:2402.17753 | supported | Treat as an external-validity set, not deterministic lifecycle ground truth. |
| E-008 | Query-only interaction can poison persistent agent memory. | MINJA, arXiv:2503.03704 | supported | Evaluate both write acceptance and later attack activation. |
| E-009 | A published agent runtime already combines append-only memory, auditability, and git-backed recovery. | Springdrift, arXiv:2604.04660 | contradicted | Supersedes any claim that git-native/auditable agent memory is wholly unprecedented; compare precise semantics instead. |
| E-010 | Lineage-based action gating is an active defense baseline for agent memory. | MemLineage, arXiv:2605.14421 | provisional | Preprint; inspect artifacts and reproduce the deterministic harness before relying on performance claims. |
| E-011 | Memory reliability under growing irrelevant history needs scale- and interaction-budget-conditioned reporting. | Scale-Conditioned Evaluation, arXiv:2605.07313 | provisional | Preprint; adopt the protocol idea and independently reproduce results. |
| E-012 | LongMemEval-V2 evaluates environment experience with histories up to 500 trajectories and includes a file-based coding-agent memory baseline. | LongMemEval-V2, arXiv:2605.12493 | provisional | Preprint; include only after checking dataset/license and released code. |
| E-013 | ForgetEval generation is deterministic for a fixed seed and covers supersession, explicit release/decay, amnesia, purge, and drift. | `deeplethe/lethe`, commit `b6053b7`, `bench/forgeteval/` | supported | Independently imported `generate(3, seed=42, distractors=4)` twice: 15 cases, byte-canonical SHA-256 `7fc71ab7b8da5c14d58e0e5e71c0f9416f0c707da1063f6b83d6e1310f7fa1b7`. This verifies generation only, not reported system scores. |
| E-014 | Belief-state threshold decay dominates rank-only decay on Temvera's controlled grid. | `experiments/configs/decay-sweep.json`, 384 deterministic cells | contradicted | Changed-fact relevant selection was 41/96 vs 56/96 and stale selection 55/96 vs 40/96; stable-old state decay abstained 41/96. Synthetic mechanism grid only. |
| E-015 | The initial generated histories can identify the necessity of exact, lexical, vector, temporal, and graph channels. | `experiments/configs/channel-ablation.json`, 48 cases | contradicted | Full and every leave-one-channel-out configuration achieved Recall@5 1.0. Add category-specific hard cases before retrieval claims. |
| E-016 | The revised hard fixture identifies one necessary channel per exact-identifier, lexical-code, vector-synonym, temporal-version, and graph-derivation category. | `temvera hard-channel-ablation`, 30 deterministic cells | supported | All-channel retrieval passed 5/5; removing the designated channel failed its category 5/5. Hand-crafted mechanism fixture only; external validity untested. |
| E-017 | Signed, tenant-scoped provenance can separate memory write acceptance from sensitive-action activation in the deterministic threat fixture. | `temvera governance-smoke --seed 17` | supported | 3/4 attacks were accepted as data, 0/4 activated, benign utility 1/1. Fixture mechanism result only; adaptive/model-mediated attacks untested. |
| E-018 | Mem0 is a required extraction/consolidation/retrieval baseline, including its graph variant. | Mem0, arXiv:2504.19413 | supported | Do not reuse the paper's self-reported LOCOMO and latency numbers as Temvera results; rerun under identical model and budget. |
| E-019 | Zep/Graphiti is a required temporal-knowledge-graph baseline. | Zep, arXiv:2501.13956 | supported | Vendor-authored preprint; inspect and pin source semantics before asserting bitemporal equivalence or performance. |
| E-020 | MemGPT/Letta is a required hierarchical virtual-context baseline. | MemGPT, arXiv:2310.08560 | supported | It addresses context-tier management; do not describe it as a relational bitemporal system without source evidence. |
| E-021 | HippoRAG 2 is a required graph-plus-passage retrieval baseline. | HippoRAG 2, arXiv:2502.14802 | supported | Use for factual/associative retrieval; lifecycle mutation, purge, and provenance defense need separate baselines. |
| E-022 | Calibrated channel weighting improves over fixed RRF on the held-out hard fixture. | `experiments/configs/fusion-comparison.json`, 16 development + 16 disjoint test examples | contradicted | Both achieved evidence recall at budget 1.0; calibrated weights were uniformly 0.5. Two channel-leakage bugs were fixed before the final run. |
| E-023 | Signed provenance alone prevents malicious actions after a configured trusted signer is compromised. | `temvera bypass-report`, deterministic adaptive probes | contradicted | Cross-tenant, tamper, and expiry replays were rejected, but a freshly signed malicious claim from the trusted key activated. Provenance proves origin, not signer honesty. |
| E-024 | Graphiti already exposes temporal graph facts with creation, validity, invalidity, and expiry timestamps and supports filtering on each field. | `getzep/graphiti`, commit `20a6728`, Apache-2.0 | supported | `remove_episode` physically deletes its episode, uniquely mentioned nodes, and edges created by that episode. Field/filter support does not by itself establish complete transaction-time `as-of` replay or deletion-lineage equivalence; neither was reproduced. |
| E-025 | Mem0 OSS records add/update/delete history while mutating or deleting the current vector record. | `mem0ai/mem0`, commit `633b035`, Apache-2.0 | supported | SQLite history stores old/new text and timestamps. Entity projection cleanup is explicitly non-fatal and may swallow per-record failures, so history availability is not evidence of atomic purge completeness or valid-time replay. Graph variant and published benchmark scores were not reproduced. |
| E-026 | Temvera's dependency-free lexical compatibility adapter passes all ForgetEval lifecycle families uniformly. | `experiments/runs/forgeteval-temvera-lexical-seed42`, 100 generated cases | contradicted | Overall 85/100: supersession, decay, purge, and drift were 20/20 each, but amnesia was 5/20. This is one seed and a local adapter result, not a comparison with published system scores. |
| E-027 | Hindsight is a required fact/belief-separated temporal graph and hybrid-retrieval baseline. | Hindsight, ACL 2026 System Demonstrations | supported | The paper describes four logical networks and temporal, graph, lexical, and vector retrieval. Its “only” and benchmark superiority claims are author-reported and not adopted without artifact reproduction. |
| E-028 | Memory addition and deletion can propagate prior experience into later agent behavior. | Xiong et al., ACL 2026 | supported | The paper identifies experience-following, error propagation, and misaligned replay. Extract artifacts and intervention details before quantitative comparison. |
| E-029 | Dynamic memory evolution and tool use are already combined in an agent-memory benchmark. | Mem2ActBench, ACL 2026 | supported | Treat the benchmark as an external action-level baseline; dataset code, license, and scorer still require inspection. |
| E-030 | Consensus validation and a separate lesson memory are active poisoning-defense baselines. | A-MemGuard, ICML 2026 OpenReview record | supported | Recently published work; inspect released artifact and rerun adaptive attacks before comparing effectiveness. |
| E-031 | Cooperative benign-looking records form an adaptive black-box memory-injection baseline. | MemIncept, ICML 2026 OpenReview record | supported | Include in threat coverage; artifact and reported attack success are not reproduced. |
| E-032 | Hindsight already combines explicit event ranges, soft invalidation with restoration metadata, change history, audit logging, graph links, and derived-record deletion cleanup. | `vectorize-io/hindsight`, commit `1549987`, MIT | supported | Source migrations document earlier orphan observations after document re-ingest and later cleanup/backfill. This is valuable contradictory evidence: deletion lineage is an active implementation problem, not a Temvera-only feature. Complete transaction-time `as-of` replay, atomic failure behavior, and benchmark scores were not reproduced. |
| E-033 | Temvera can recover current-tree redaction receipts and multi-key cryptographic purge after an injected post-mutation receipt failure. | `tests/test_store.py`; `tests/test_protected_store.py` | supported | Durable journals contain identifiers and hashes, never plaintext or keys; recovery is idempotent in the tested failure point. This is crash recovery, not multi-filesystem atomicity or proof about undeclared replicas. |
| E-034 | A-MemGuard has an MIT-licensed public evaluation artifact with seeded EHRAgent execution and selectable consistency backends. | `TangciuYueng/AMemGuard`, commit `dd92f7f` | supported | Full reproduction requires AgentPoison-derived data, large pinned ML/GPU dependencies, external checkpoints, and an API or local model. No reported ASR or utility number was reproduced. |
| E-035 | The experience-following paper releases an AgentDriver patch rather than a self-contained licensed experiment. | `yuplin2333/agent_memory_manage`, commit `4dc29e3` | supported | README requires copying an upstream repository, preparing its data, inserting API credentials, and manually changing distribution-shift loading; no repository license was found. Do not redistribute or treat published gains as reproduced. |
| E-036 | LongMemEval's official QA and retrieval evaluations have different abstention semantics. | `xiaowu0162/LongMemEval`, commit `9e0b455`, MIT code | supported | QA uses `gpt-4o-2024-08-06` as judge and reports abstention accuracy; retrieval scoring omits the 30 no-evidence abstention instances. Do not compare retrieval recall and QA abstention as if they covered identical cases. |
| E-037 | The frozen Temvera lifecycle splits are byte-reproducible and identifier-disjoint under the current generator. | `data/fixtures/lifecycle-v0`; dataset verifier tests | supported | An earlier card incorrectly claimed disjoint labels while IDs were shared across seeds; split namespaces and a leakage test corrected it. The fixture is fully synthetic and licensed under Apache-2.0; no third-party data is included. |
| E-038 | Temvera's accepted local experiment runs have byte-verifiable configurations, environments, and results. | `temvera seal-run`; `temvera verify-run`; `tests/test_experiment.py` | supported | Lifecycle, lifecycle-grid, and ForgetEval runs were born sealed; five earlier mechanism runs were sealed after generation without rewriting raw results. Checksums detect later byte changes, but do not independently reproduce results or authenticate the machine. Learned-vector evaluation is excluded because its model artifact was not downloaded. |
| E-039 | On the original 12-case lifecycle smoke fixture, non-temporal baselines fail exact temporal state while the oracle remains exact. | `experiments/runs/lifecycle-9406331b97a3`; `error-analysis.json` | superseded | The evaluator omitted fully purged subject/attribute pairs and transaction-time cases. E-042 and E-043 replace this result with corrected case construction; retain this row as negative provenance rather than citing its scores. |
| E-040 | Springdrift's experiment-5 script can silently evaluate zero-vector fallback retrieval when Ollama is unavailable. | `seamus-brady/springdrift`, commit `19b52b9`; `evals/experiment-5/run_eval.py` | supported | Two fallback runs were byte-identical (`experiment5_results.json` SHA-256 `b21cf7e...`, CSV `933776f...`) because NumPy seed 42 is fixed, but point estimates differed from committed embedding-backed results. The decay property checks passed, yet they duplicate the formula in Python rather than executing the Gleam runtime function. Do not label this an end-to-end Springdrift reproduction. |
| E-041 | The initial exact lifecycle grid kept the oracle exact while non-temporal baselines degraded with revisions. | `experiments/runs/lifecycle-grid-v1`, 162 system-condition rows | superseded | Its case constructor omitted explicit transaction-time cases and fully purged pairs. The immutable run is retained, but corrected grid E-042 replaces its numbers. |
| E-042 | The corrected revision-only lifecycle oracle remains exact across a 3-seed × 3-scale × 3-revision grid. | `experiments/runs/lifecycle-grid-v2`, 162 system-condition rows | superseded | This fixed the evaluator's transaction-time omission but still held operation topology fixed. E-044 adds a seeded mixed-operation profile; retain v2 as immutable correction provenance. |
| E-043 | Explicit purge and expiry cases distinguish current-state, raw-context, and bitemporal semantics in the operation suite. | `experiments/runs/lifecycle-operations-v1`, 3 seeds × 12 cases | supported | Oracle exact accuracy was 1.0 in purge, expiry-boundary, transaction-as-of, and valid-time categories. Append/full/recent/BM25 scored 0 on expiry and purge exactness; LWW scored purge 1.0 but expiry 0 because it respects current deletion while ignoring historical valid time. Seed CIs are degenerate because suite topology is fixed. Reconfirmation metadata is covered by state tests, not query-result metrics. |
| E-044 | The exact oracle remains correct under both revision-only and seeded mixed reconfirm/expire/purge workload profiles. | `experiments/runs/lifecycle-grid-v4`, 324 system-condition rows | supported | For each of 2 profiles × 3 scales × 3 revision counts × 3 seeds, oracle exact/recall/stale remained 1/1/0. Mixed-profile descriptive means: append/full exact 0.257, recall 0.890, stale 0.644; recent/BM25 exact 0.562, stale 0.438; LWW exact 0.586, stale 0.414. These are synthetic profile summaries, not population estimates; only three seeds and two hand-specified operation distributions were used. |
| E-045 | The local reproducibility package contains ten allowlisted sealed runs that pass aggregate checksum verification. | `experiments/accepted-runs.json`; `temvera verify-artifacts` | supported | Accepted files contain synthetic/local outputs only, including the sealed security evaluation, and are covered by Apache-2.0. Verification proves byte integrity relative to each run manifest, not independent recomputation, authorship, or archival durability. DOI archival is deferred under D-009. |
| E-046 | Mem2ActBench's official ACL page advertises code and data, but its linked repository was not publicly retrievable at verification time. | ACL Anthology 2026.acl-long.370; linked `Cantaloupe-M/Mem2ActBench` URL | supported | On 2026-07-16 the GitHub URL returned 404 and unauthenticated clone failed, so no commit, license, scorer, or data checksum was verified. This is a time-specific availability result, not evidence that the artifact never existed or will remain unavailable. |
| E-047 | The accepted local research artifact can be packaged deterministically and verified from an internal per-file manifest. | `temvera artifact-package`; `tests/test_artifact.py` | supported | The Apache-2.0 candidate contains more than 300 files and ten sealed runs, includes `LICENSE`, excludes the superseded license placeholder, and verified successfully. Determinism is tested on a fixture. This is packaging integrity, not a public archive, DOI, or independent reproduction. |
| E-048 | The repository's approved local release scope uses the Temvera name and Apache-2.0 while deferring participant recruitment, learned-model download, and public DOI archival. | `LICENSE`; `CITATION.cff`; `docs/decisions.md` D-007–D-009 | supported | This records repository governance and scope decisions, not trademark clearance, human-study findings, learned-model evidence, or archival durability. Third-party materials remain governed by their own terms. |

| E-049 | The exact oracle stays correct under a wider 3-profile × 3-scale × 3-revision × 10-seed grid, including a high-churn profile, while non-temporal baselines degrade further. | `experiments/runs/lifecycle-grid-robust-v1`, 1620 system-condition rows, sealed and verified | supported | Strengthens E-044 by raising seeds 3→10 (per-condition bootstrap CIs now over ten seeds) and adding a `high_churn` profile (reconfirm 0.8, expire 0.4, purge 0.4). Oracle exact/recall/stale was 1/1/0 in every one of 90 conditions (270 oracle cells); high-churn descriptive means: append_only exact 0.255 / stale 0.662, recent/BM25 exact 0.532 / stale 0.468, LWW exact 0.580 / stale 0.420. Still fully synthetic with hand-specified operation distributions; "contradiction density" is approximated by operation-mix intensity and revision count, not concurrent competing valid facts. Run is sealed/verified but not yet allowlisted into `accepted-runs.json` (release-gate step). |

| E-050 | A learned dense channel resolves held-out synonyms that the dependency-free hashing channel cannot. | `temvera vector-synonym-compare`; `src/temvera/synonym_eval.py`; `experiments/runs/vector-synonym-compare-v1.json` | supported | On 10 paraphrase pairs whose terms are absent from `index.SEMANTIC_ALIASES`, learned `BAAI/bge-small-en-v1.5` recall@1 was 1.0 while the hashing channel was 0.0. Closes E-038's learned-model exclusion under D-011 (fastembed on CPU). Small hand-crafted set, single MIT-licensed model, recall@1 only; the 0.0 reflects the hashing channel's intentional alias-table-only design, so this quantifies the alias-coverage gap, not a general retrieval-SOTA claim. Deterministic for a fixed model; output is an unsealed JSON file, not a sealed run. |
| E-051 | The deterministic artifact archive reliably detects tampering. | `tests/test_artifact.py::test_artifact_archive_is_deterministic_and_detects_tamper` | provisional | The tamper assertion is flaky: `verify_artifact_archive` recomputes per-file checksums after gzip decompression, so a single bit flipped in gzip framing/padding can leave decoded content unchanged and pass verification. Observed intermittently in full-suite runs; passes in isolation. Fix should verify envelope integrity (e.g. archive-level digest) or flip a byte inside a decompressed member. Not introduced by the systems-paper work; logged for the release-hardening gate. |

| E-052 | In a first identical-history comparison, Mem0 answers from a consolidated current-state view and cannot recover historical valid-time, transaction-time `as-of`, or expiry-boundary state, while the oracle stays exact — but a synthetic-token naming confound must be controlled first. | `experiments/runs/external-mem0-smoke-v1` (synthetic) and `-v2` (naturalized), sealed and verified; `src/temvera/mem0_adapter.py`, `external_experiment.py` | provisional | Mem0 `0.1.118`, `gpt-4o-mini` (temp 0), `text-embedding-3-small`, single seed 17, 12 events / 13 cases, single-pass ingestion (no per-query transaction-time replay). Injective naturalization (`naturalize_events`) raised Mem0 exact 0.231→0.462, isolating ~half of the synthetic failure as tokenization rather than semantics. Residual failures concentrate in transaction_as_of/valid_time (returns latest consolidated value) and expiry_boundary (abstention 0.0 — cannot withhold an expired fact); Mem0 sometimes retains temporal metadata it does not query over. Oracle reference exact 1.0 under identical ingestion. This is a pilot proving the harness runs end-to-end against a deployed system and showing the *direction* of divergence — not a multi-seed population result. Per-query transaction-time replay, more seeds/scales, and Zep/Graphiti remain (D-010). |

| E-053 | A sealed 8-seed × 3-scale × 2-profile identical-history comparison shows Mem0 answers transaction-time-scoped current queries well but fails historical valid-time, expiry, and purge, while the oracle stays exact and degrades not at all. | `experiments/runs/external-mem0-grid-v1`, 48 cells (96 system-condition rows), sealed and verified | supported | Overall Mem0 exact 0.607 / stale-use 0.379 / abstention 0.596 vs oracle 1.0 / 0 / 1.0. Mem0 exact by category: transaction_as_of **0.949**, valid_time **0.346**, expiry_boundary **0.062**, purge **0.121** — i.e. it returns the latest consolidated value and cannot recover past valid-time state, withhold an expired fact, or honor a purge (direct C-B deletion-completeness evidence). Mem0 exact degrades with scale (≈0.70 small → ≈0.52 large), 95% bootstrap CIs over 8 seeds per condition. Backbone `gpt-4o-mini` (temp 0), Mem0 `0.1.118`, `text-embedding-3-small`, forward-checkpoint transaction-time replay, naturalized surface names (E-052). Deterministic value-substring scorer; Mem0 sometimes extracts temporal metadata it does not query over. Single backbone and synthetic histories; strengthens and supersedes the single-seed pilot E-052. Graphiti/Zep and public-conversation external validity (E7) remain. |

| E-054 | Under identical histories, Graphiti retains temporal edges (high evidence recall) but its default retrieval does not filter by valid-time, returning superseded facts alongside current ones — a distinct failure mode from Mem0, and it likewise cannot honor expiry/purge. | `experiments/runs/external-graphiti-grid-v1`, 6 cells, sealed and verified; Neo4j 5.26, `graphiti-core 0.29.2` | supported | Graphiti overall exact 0.224 / recall **0.873** / stale-use **0.561** / abstention 0.0 vs oracle 1/1/0/1. By category exact: transaction_as_of 0.444, valid_time 0.000, expiry_boundary 0.000, purge 0.062. Contrast with Mem0 (E-053): Mem0 consolidates to a single current value (forgets history); Graphiti preserves edges but returns stale + current together (high recall, low exact). **Fairness caveats:** retrieval used edge cosine-similarity only (Kuzu's FTS is broken so the config was kept consistent on Neo4j too) and did **not** apply Graphiti's `valid_at` SearchFilter, so this is Graphiti's *default* behavior, not its ceiling — testing temporally-filtered search is a required refinement. 6 cells (3 seeds × 2 scales × 1 profile; smaller than the Mem0 grid because `add_episode` is far heavier); occasional extraction warnings ("Target entity not found"); `gpt-4o-mini` backbone; substring scorer. Ran on a rootless tarball Neo4j (no Docker). |

| E-055 | Applying Graphiti's own valid-time `SearchFilter` improves its bitemporal accuracy. | `experiments/runs/external-graphiti-filtered-v1` (6 cells, sealed/verified) vs `external-graphiti-grid-v1`; Neo4j 5.26, `graphiti-core 0.29.2` | **superseded — invalid measurement** | **Retracted 2026-07-17: cross-run contamination.** Graphiti cell `group_id`s repeat across runs and the Neo4j `reset()` did not wipe the group, so this filtered run queried a graph that still held the earlier default run's episodes and edges (e.g. cell `e1-mixed_lifecycle-e3-r2-s7` accumulated 36 episodes where one run produces ~12). Its numbers therefore mix two ingestions and must not be cited. The default run E-054 is unaffected because it executed first. Re-measured cleanly in E-057 after adding a per-group wipe. Original (invalid) reading follows for provenance only: Enabling the native filter (`valid_at <= V AND (invalid_at IS NULL OR invalid_at > V)`) **degraded** every metric: exact 0.224→0.031, evidence recall 0.873→0.049, stale-use 0.561→0.960. Diagnosis (direct Cypher over the 80 produced edges): the extracted temporal metadata is unreliable — 26/80 edges carry **no** `invalid_at` even after supersession, so stale facts satisfy the filter at any event time, while 4/54 dated invalidations are strictly contradictory (`invalid_at <= valid_at`, e.g. valid 2025-03-20 / invalid 2025-03-14) and wrongly exclude correct edges. Only 10.4% of filtered answers were empty, so the loss is wrong-fact selection, not abstention. **Fairness notes:** `created_at`/`expired_at` store wall-clock ingestion time (2026), not the simulated transaction time, so they cannot express an `as-of` bound and were excluded; that axis is enforced exactly by forward-checkpoint replay. 0/80 edges had NULL `valid_at`, so the filter was not unfairly excluding undated edges. **Confound:** extraction used `gpt-4o-mini`; a stronger extractor may populate temporal fields better, so this bounds the *default pipeline as configured*, not Graphiti's ceiling. One filter formulation, 6 cells, single backbone. |

| E-056 | After an ingested purge, Temvera's rebuildable projections contain no purged payload while both external systems retain it in their live serving paths. | `experiments/runs/purge-residual-v2` (sealed/verified); `src/temvera/purge_residual.py`; supersedes the unfair `purge-residual-v1` | supported | Purged values `Accra, Cairo, Oslo`. **Temvera 3 total**: raw JSONL ledger 3 (documented limitation D-005; the encrypted profile addresses it via key destruction, E-033), Markdown/lexical/vector/graph projections **0 each**. **Mem0 14**: exposed memories 2, Qdrant vector store 2, SQLite history 10. **Graphiti 27**: edge facts 5, entity nodes 13, raw episode bodies 9. The claim is *which store classes* retain, not the integers: Temvera's residual sits only in the append-only audit ledger, whereas both external systems retain inside retrieval-reachable stores, so a later query can resurface deleted content — consistent with answer-level purge failures (E-053 0.121, E-054 0.062). **Corrections/caveats:** v1 reported Mem0 as 0 because it scanned only API-exposed memories while scanning Graphiti's raw database; v2 adds Mem0's vector store and history DB and isolates `history_db_path` so the global `~/.mem0/history.db` cannot attribute other runs' rows here. Counts are substring occurrences over differently-shaped stores and are not magnitude-comparable across systems. Single seeded history (12 events, 3 purges), `gpt-4o-mini`; LLM extraction is not bit-reproducible, so counts vary between runs (v1 Graphiti 21 vs v2 27). No claim is made about backups, OS caches, or provider-side copies. |

| E-057 | On uncontaminated runs, Graphiti's native valid-time `SearchFilter` trades recall for precision: it raises exact accuracy and cuts stale use, but still leaves historical valid-time and deletion largely unanswered. | `experiments/runs/external-graphiti-filtered-v2` (6 cells, sealed/verified, per-group wipe + unique group prefix) vs `external-graphiti-grid-v1` | supported | Default → filtered: exact **0.224 → 0.318**, stale-use **0.561 → 0.360**, evidence recall **0.873 → 0.318**. So the filter genuinely helps precision, and the retracted E-055 ("degrades everything") was an artifact of cross-run contamination — this row **reverses** that conclusion. By category the filtered run still fails the semantics that matter: valid_time 0.093, expiry_boundary 0.067, purge 0.306, transaction_as_of 0.491, against oracle 1.0 throughout. Isolation verified directly in Neo4j (12 episodes per small cell, versus 36 under the contaminated grouping). `gpt-4o-mini` extraction, one filter formulation (`valid_at <= V AND (invalid_at IS NULL OR invalid_at > V)`); `created_at`/`expired_at` are wall-clock ingestion times and cannot express an `as-of` bound, so transaction time is enforced by forward-checkpoint replay. 6 cells, single backbone; LLM extraction is not bit-reproducible. |

| E-058 | A stronger extraction model closes Graphiti's bitemporal gap. | `experiments/runs/external-graphiti-filtered-gpt4o-v2` vs `external-graphiti-filtered-v2`, matched cells (seeds 7/17 × 2 scales, filter enabled, per-group isolation verified) | contradicted | Replacing `gpt-4o-mini` with `gpt-4o` for extraction produced no material improvement: exact 0.343 → 0.370, recall 0.343 → 0.389, stale-use 0.353 → 0.322. Decisively, the categories that define the gap did not move — valid_time **0.111 → 0.111** (identical), expiry_boundary **0.000 → 0.000** — while purge got *worse* (0.296 → 0.176) and only transaction_as_of improved (0.514 → 0.625). Oracle remains 1.0 on the same cells. This removes the main confound in E-057: the ceiling is architectural (no transaction-time `as-of` axis; `invalid_at` unreliable/absent on superseded edges) rather than a weak-extractor artifact. Caveat: 4 matched cells and 2 seeds, so ±0.03 differences are within noise — the load-bearing observations are the *unchanged* valid_time/expiry categories, not the small aggregate deltas. Single filter formulation; one alternative extractor tested, not a sweep. |

Status vocabulary: `supported`, `contradicted`, `provisional`, `unverified`,
`blocked`, `superseded`.
