# Pre-submission audit, 2026-08-19

Every numeric claim in `paper/main.tex` was recomputed from the sealed runs and
traced to a specific artifact. Method: micro and macro aggregation, matched and
unmatched cell sets, and per-budget sweeps were computed for every run that
carries a transcript, then each paper number was searched for across all of
them. A number that no aggregation reproduces is reported as unsupported rather
than assumed correct.

The measurements hold. The defects are in how the paper cites them: five
numbers in one subsection come from runs the paper does not use — one of which
it explicitly disowns two paragraphs later — and the subsection that would be
hardest for a reviewer to check rests on a run that does not ship.

## S1 — §5.2 quotes a superseded run and a different experiment

`\textbf{Mem0 consolidates}` and `\textbf{Graphiti retains but does not filter}`
report numbers that no current run produces:

| Paper | Traced to | Correct value (runs the paper uses) |
|---|---|---|
| Mem0 `transaction_as_of` $0.956$ | `external-mem0-grid-v1` (0.9561) | **0.952** |
| Mem0 `valid_time` $0.374$ | `external-mem0-grid-v1` (0.3744) | **0.364** |
| Graphiti exact $0.194$ | `external-graphiti-scale-v1` (0.1942) | **0.196** |
| Graphiti recall $0.944$ | `external-graphiti-scale-v1` (0.9439 macro) | **0.937** |
| "identical at $k{=}1$ ($0.385$)" | both above were 0.3846 | **0.379 vs 0.354 — not identical** |

`external-mem0-grid-v1` is the shared-history-database run that the *same
subsection* describes as corrupted under contention and re-run. The paragraph
announcing the correction quotes the uncorrected numbers.

The qualitative claim survives: at $k{=}1$ the Wilson intervals overlap heavily
(Mem0 $[0.314, 0.449]$, Graphiti $[0.290, 0.423]$), so "indistinguishable" is
supported. "Identical", and the single shared figure $0.385$, are not.

## S2 — §5.6 rests on a run that does not ship

Every number in External Validity comes from `longmemeval-e7-v2` (60 instances,
6 question types). Its seal verifies and its values match the paper exactly
(0.7238, 0.6221, 0.3136, 0.2867, 0.2208, 0.1512). But the run is **not in
`accepted-runs.json`**, is matched by the `experiments/runs/*` ignore rule with
no allowlist entry, and is therefore **untracked by git and absent from the
artifact package**. The two accepted LongMemEval runs hold 6 and 12 instances
across two question types and cannot produce the reported table.

For a track whose reviewers are a Reproducibility Committee, this is the most
serious finding: the section is unverifiable as shipped, and the run would be
lost to a clean checkout.

## S3 — Table 1's caption and body are different experiments

`tab:baselines` is captioned *"Internal baselines over 27 conditions (3 profiles
× 3 scales × 3 revision counts) × 10 seeds = 270 cells per system … deliberate
strawmen"* but its body is the seven **external** configurations over 484 cases.
Consequences:

- §5.1's prose cites this table for numbers that are not in it. Those numbers
  are themselves correct — `lifecycle-grid-robust-v1` gives append-only and
  full-context 0.915 recall / 0.272 exact, high-churn 0.255 / 0.662, oracle
  1/1/0 over exactly 27 conditions × 10 seeds — so the internal-baselines table
  is *missing*, not wrong.
- The external summary is duplicated between `tab:baselines` and `tab:reader`.

## S4 — One unrecomputable statistic, and one auditor error

- **"rank~1 in 99.4% of cases"** — no transcript carries a rank field, but the
  `answer` field preserves the returned items in rank order, so the statistic is
  recomputable under an explicit rule: the first returned item names the queried
  subject in **442/484 = 91.3%** of cases on the hybrid run the paragraph
  describes. The reported 99.4% is closest to the scale run (99.79%). Fixed by
  restating the rule and the hybrid figure.
- **Graphiti filtered "valid-time remains $0.093$" — this finding was wrong.**
  $0.093$ is the *cell-averaged* valid-time of `external-graphiti-filtered-v2`
  (0.0926), and E-057's other per-category figures match cell-averaging exactly
  (0.067, 0.306, 0.491). The first pass searched per-category **micro** rates
  only and therefore missed it. The number is supported; only its aggregation
  was unlabelled.

  The same error nearly propagated: the adjacent \texttt{gpt-4o} comparison
  ($0.111 \to 0.111$, $0.514 \to 0.625$, $0.296 \to 0.176$) reproduces the
  cell-averaged values to four decimals and was very close to being "corrected"
  into wrong numbers. **Verifying a number against one aggregation and declaring
  it unsupported is itself an unsound check**; every later claim in this audit
  was tested under both.

## S8 — Four named baselines are two

`append_only` and `full_context` are identical on all 270 cells, as are
`bm25_at_1` and `recent_context`. The first pair is identical by construction
(returning the whole history is one retrieval under two names); the second is a
property of these histories, where BM25's top hit is always the most recent
turn. The paper named five independent baselines. Table~\ref{tab:baselines} now
shows them merged and says why.

## S9 — Section 4 and Section 5.2 cited a retracted run

The edge-metadata diagnosis ("absent on 26 of 80 superseded edges … 4 of 54
dated invalidations") comes from **E-055**, which the ledger marks *superseded —
invalid measurement* over cross-run contamination and states explicitly **must
not be cited**. Those counts were over a graph holding two ingestions. The
mechanism they illustrate is still the right explanation, so both sections now
state the mechanism and withhold the counts, pointing at the retraction.

## S5 — Cognee latency quotes 38-sample smoke runs

$0.76$\,s / $7.93$\,s / $5.00$\,s come from `external-cognee-v1` and
`external-cognee-chunks-v1` (n = 37–38). The grid runs used for every other
Cognee number give **0.647 / 6.906 / 4.534** (n = 477/484). The conclusion —
Cognee inverts the write/read split — holds under either, but the paper switches
sample without saying so, and uses the smaller one.

## S6 — Stale system counts

- Setup, *Systems*: names only "Mem0 `0.1.118` and Graphiti `0.29.2`". LangMem,
  Cognee `1.4.2` and Hindsight are missing.
- Limitations: "Two external systems, one backbone" against the abstract's five.
- `tab:reader` is captioned "Five systems, and two of them read both ways" but
  omits both Hindsight rows, so it shows five rows with only one system read
  both ways.
- Limitations repeats its opening sentence nearly verbatim one paragraph later.

## S7 — Minor

- **Aggregation is mixed silently.** Tables declare case-weighted (micro) rates;
  the adjacent Graphiti-filter paragraph is macro (0.224 / 0.561 / 0.873 are all
  cell-averaged). Both are defensible; neither is labelled.
- **Figures are not byte-reproducible.** Regeneration produces identical text
  content but different bytes (PDF timestamps).
- **Coincidental collisions.** Mem0 0.603 / LangMem 0.576 round identically to
  internal last-write-wins 0.6028 / BM25 0.5763. Distinct measurements — they
  diverge at the 4th decimal — but adjacent tables invite the wrong reading.
- **Five ledger-cited runs are untracked**: `lifecycle-grid-v1`,
  `lifecycle-grid-v2`, `lifecycle-operations-v1`, `lifecycle-9406331b97a3`,
  `longmemeval-e7-v2`. Two carry claims still marked *supported*
  (`longmemeval-e7-v2`, `lifecycle-operations-v1`) and are now accepted and
  allowlisted; the other three back superseded rows and stay local, which is
  recorded here rather than left implicit.
- **The newly shipped run redistributes derived third-party content.**
  `longmemeval-e7-v2` seals a `mem0-history.db` of 2023 memories extracted from
  LongMemEval conversations. It cannot be dropped without breaking that run's
  checksums, and the corpus's MIT licence permits it, so it ships and §7 now
  says so instead of claiming nothing third-party is redistributed.
- `refs.bib` carries two uncited entries (`locomo`, `sql2011`).

## Verified clean

Recomputed and matching to every reported digit:

- All 28 accepted runs seal-verify; all are tracked by git.
- Tables 2 and 3 (external grid, reader): every cell, including denominators.
- §5.1 internal baselines, and the 27-condition / 270-cell design.
- Deletion residuals: NL 3 / 15 (3,3,9) / 18 (3,9,6); API 12 (history) / 3
  (entity nodes).
- Cost table: 1035 / 949 samples, 4.733\,s / 0.300\,s, p50 and p95, \$0.5319
  total, ~16× ratio, \$0.0005 per ingested turn, embeddings 0.2% of spend.
- Scale sweep 0.610 → 0.527 → 0.539.
- LongMemEval abstention-exclusion reordering (0.810 above 0.794) and the
  retracted distractor comparison (0.125 → 0.167).
- The abstract's budget claim, in direction: overlapping Wilson intervals.
- Artifact package is byte-identical across builds and verifies.
- Citations: no undefined keys; 128 tests and `ruff` pass.

## Resolution

All findings were remedied on 2026-08-20 in the order above.

| # | Action |
|---|---|
| S1 | Seven figures restated from the runs the paper uses; "identical" replaced by overlapping Wilson intervals (0.379 vs 0.354) |
| S2 | `longmemeval-e7-v2` accepted, allowlisted and shipped; §7's redistribution claim corrected |
| S3 | Internal-baselines table restored under its caption; duplicated external summary removed |
| S4 | Rank statistic recomputed and restated as 91.3% under an explicit rule; the 0.093 finding withdrawn as an auditor error and the aggregation labelled |
| S5 | Cognee latency requoted from the 477/484-sample grid runs |
| S6 | System counts corrected in Setup and Limitations; Limitations de-duplicated; both Hindsight rows added to Table 3 |
| S7 | Aggregation stated in Setup; the 0.603/0.576 coincidence called out; figures made byte-reproducible; `lifecycle-operations-v1` accepted |
| S8 | Merged baselines shown as merged, with the reason |
| S9 | Retracted-run counts removed from §4 and §5.2; mechanism retained |

Two uncited `refs.bib` entries (`locomo`, `sql2011`) are left in place; BibTeX
does not emit them.

**The audit is now a standing check.** Finding this by hand once is worth
little if the next edit reintroduces it, so `scripts/verify_paper_claims.py`
holds all 82 reported figures, each pairing its printed form with a recomputation
from a sealed run, anchored to the sentence or row that prints it, and declaring
its aggregation. `pytest` runs it. Both drift scenarios this audit found --- a
prose number edited away from a correct table, and a latency requoted from a
smaller run --- were replayed against the guard and are caught.
